# Copy this to .env.production on the server and set your production API URL.
# Next.js loads .env.production when running `next build`.
# This ensures the frontend talks to your API (not localhost).
NEXT_PUBLIC_API_BASE_URL=https://layerpainter-api.margies.app
